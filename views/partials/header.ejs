<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/dashboard">ระบบจัดการข้อมูล</a>

    
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
        
        <ul class="navbar-nav ml-auto d-flex align-items-center">
            
            <% if (user) { %>

                <li class="nav-item">
                    <span class="navbar-text mr-3">
                        สวัสดี, <strong><%= user.first_name %></strong> (<%= user.role.toUpperCase() %>)
                    </span>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-bars"></i> เมนูหลัก
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                          
                        <% if (user.role === 'admin') { %>
                            <a class="dropdown-item" href="/admin/manage-users">
                                <i class="fas fa-users-cog"></i> จัดการผู้ใช้งาน (CRUD)
                            </a>
                        <% } %>

                        <% if (user.role === 'admin') { %>
                            <a class="dropdown-item" href="/admin/manage-permissions">
                                <i class="fas fa-shield-alt"></i> ตั้งค่าสิทธิ์การเข้าถึง
                            </a>
                        <% } %>

                        <% if (user.role === 'admin') { %>
                            <div class="dropdown-divider"></div>
                        <% } %>


                        <% if (user.role === 'admin' || user.permissions.includes('view_member_list')) { %>
                            <a class="dropdown-item" href="/member-list">
                                <i class="fas fa-users"></i> ดูรายการ Member
                            </a>
                        <% } %>
                        <% if (user.role === 'admin' || user.permissions.includes('add_products')) { %>
                            <a class="dropdown-item" href="/products">
                                <i class="fas fa-plus"></i> เพิ่มรายการสินค้า
                            </a>
                            <div class="dropdown-divider"></div>
                        <% } %>


                        <% if (user.role === 'admin' || user.permissions.includes('edit_own_profile')) { %>
                            <a class="dropdown-item" href="/profile">
                                <i class="fas fa-user-edit"></i> แก้ไขข้อมูลส่วนตัว
                            </a>
                        <% } %>

                    </div>
                </li>

                <li class="nav-item">
                    <a href="/logout" class="btn btn-outline-danger my-2 my-sm-0 ml-2">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </a>
                </li>
                
            <% } %>
            
        </ul>
    </div>
</nav>